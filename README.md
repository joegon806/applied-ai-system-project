# The Mood Machine

The Mood Machine is a text classifier that classifies the mood of a text as positive, negative, neutral, or mixed. The classification can be determined based on either a determinisitic rule-based model or a machine learning model.

The final project extends The Mood Machine's Rule-based model to have checks for reliabilty and confidence scores based on those checks. A test suite for the implemented functionality is also added.

# Architecture Overview

In the Rule-based Model, the model first runs evaluate_rule_based, which takes the SAMPLE_POSTS from dataset.py and applies MoodAnalyzer to them to predict their moods, and then applies reliability.py to them to generate their confidence scores. Then the model lets the user enter their own phrases, and applies MoodAnalyzer to predict their moods and applies reliability.py to generate their confidence scores.

In the Machine Learning-based Model, the LogisticRegression model is trained on the vectorized SAMPLE_POSTS and their TRUE_LABELS, and uses this data to predict the label of the user's entered posts.

The file reliability.py applies 6 reliability checks to generate a reliability report. Then it compares the sample posts' given labels to the labels generated by the rule-based model to generate a rule-based evaluation report.

The test suite performs automated tests on the functionality of reliability.py.

# Setup Instructions

To run the Rule-based model, run "python main.py" in the terminal. When prompted, type the post you want classified and press enter.
To run the Machine Learning-based model, run "python ml_experiments.py" in the terminal. When prompted, type the post you want classified and press enter.
To view the reliability report and rule-based evaluation report, run "python reliability.py".
To run the automated reliability tests, run "pytest test_predict_reliable.py".

# Sample Interactions

Rule-based model:

```
You: **This is a good day**
Model: positive (54% confidence)
You: I hate fast food
Model: negative (52% confidence)
You: I love and hate you so much
Model: uncertain (35% confidence, leaning mixed)
  (low confidence — the signals for this text are weak or conflicting, so the model will not commit to a mood.)
```

ML-based model:
```
You: This is a good day
ML model: negative
You: I hate fast food
ML model: negative
You: I love and hate you so much
ML model: positive
```

# Design Decisions

The function predict_reliable() in reliability.py computes a confidence from three reliability signals: 
- consistency (case / whitespace / trailing-punctuation don't flip the label),
- stability (adding a positive word doesn't lower the mood score, and adding a negative word doesn't increase it),
- and strength (how much positive and negative words cancel each other out),
and if the confidence lands below a set threshold of uncertainty, the function abstains from giving the label confidently, labeling the mood as "uncertain". Confidence is calculated as (0.45 * stability + 0.55 * strength) * consistency: stablity and strength are added together because they form the base for confidence, but are not too significant, and both are about equal in weight. Consistency, however, is multiplied into the formula, giving it significant weight. This is because consistency, which involves changing insignificant parts of the text, should not have any effect on the label. If changing something insignificant does affect the label, then it's the result a significant reliability failure, and the model should not be confident in that label.

One trade-off I made when developing this project's expansion is that I neglected to add many more labels, sample posts, positive words, or negative words to the dataset. I made this choice because I believed adding words would be a sisyphean task, in which I wouldn't know how much data I would need to add before I could be confident enough in my model, especially considering edge cases like slang or word conjugation. So instead, I opted to create a confidence generator for the model that I already have.

# Testing Summary

What worked with this model is that obviously positive or negative posts (e.g., no words of the opposite mood) get labeled correctly. One thing that didn't work with this model is that I found that mixed posts (e.g. posts with both positive and negative posts) tended to be flagged as uncertain with low confidence. I also found that the model still produces wrong answers if their computed uncertainty is lower than the uncertainty threshold, which taught me that such a mood detection algorithm is not a cut-and-dry technique but instead requires careful human evaluation and adjustment, such as adjusting the threshold of uncertainty in this model.

# Reflection

This project taught me that using and creating an AI must involve a healthy amount of human collaboration, including supervising the AI's suggestions and adjusting details as needed.

# Portfolio Artifact

GitHub Repo Link: https://github.com/joegon806/applied-ai-system-project

This applied AI project demonstrates my experience in working with AI and creating AI. For this project, I collaborated with the chatbot to help me design, understand, and implement the new features. I used skills in prompting the chatbot, reviewing its suggestions, accepting and even rejecting its suggestions when needed, and modifying its edits. Additionally, this project shows my skills in developing an AI model. I implemented functions to check the model's reliability, implemented a confidence measure based on this reliability and other checks, and implemented a test suite to verify this new functionality.

# Reproducible Execution

```
C:\Users\joego\CodePath AI Eng Projects\Projects\applied-ai-system-project>python main.py

=== Interactive Mood Machine (rule based) ===
Type a sentence to analyze its mood.
Type 'quit' or press Enter on an empty line to exit.

You: I'm happy about completing this project!
Model: positive (58% confidence)
You: Although I'm sad about ending this class...
Model: negative (59% confidence)
You: Love it or hate it, I suppose.
Model: uncertain (38% confidence, leaning mixed)
  (low confidence — the signals for this text are weak or conflicting, so the model will not commit to a mood.)
You: quit
Goodbye from the Mood Machine.

Tip: After you explore the rule based model here,
run `python ml_experiments.py` to try a simple ML based model
trained on the same SAMPLE_POSTS and TRUE_LABELS.

C:\Users\joego\CodePath AI Eng Projects\Projects\applied-ai-system-project>python reliability.py
=== Mood Machine Reliability Report ===

[PASS] Dataset length alignment
    Aligned: 14 posts and 14 labels.

[PASS] Label-map coverage
    All 9 human label(s) map onto valid model labels.

[PASS] Label-space validation
    All 4 label(s) used in TRUE_LABELS are producible by the model. Accuracy is measured fairly.

[PASS] Determinism (same input -> same label)
    All 14 posts gave a stable label across 5 runs.

[PASS] Invariance (harmless edits keep the label)
    All 14 posts survived case, whitespace, and trailing-punctuation edits unchanged.

[PASS] Directional monotonicity (score moves the right way)
    Score moved in the expected direction for all 14 posts.

All reliability checks passed.
=== Rule Based Evaluation on SAMPLE_POSTS ===
"I love this class so much" -> positive (56% confidence), true=positive
"Today was a terrible day" -> negative (54% confidence), true=negative
"Feeling tired but kind of hopeful" -> negative (56% confidence), true=mixed
"This is fine" -> neutral (73% confidence), true=neutral
"So excited for the weekend" -> positive (54% confidence), true=positive
"I am not happy about this" -> negative (48% confidence), true=negative
"im dead 💀" -> neutral (73% confidence), true=positive (human: slang)
"I absolutely love getting stuck in traffic" -> positive (57% confidence), true=negative (human: sarcastic)
"I feel very strongly about this." -> neutral (73% confidence), true=neutral (human: ambiguous)
"What do you mean" -> neutral (73% confidence), true=neutral (human: questioning)
"It is what it is" -> neutral (73% confidence), true=neutral (human: ambiguous)
"I told you so" -> neutral (73% confidence), true=negative (human: passive_aggressive)
"Oh great, another Monday" -> positive (54% confidence), true=negative (human: sarcastic)
"Love the price but hate the service" -> uncertain (35% confidence, leaning mixed), true=mixed

Rule based accuracy on SAMPLE_POSTS: 0.57 (8/14)
Abstained (uncertain): 1/14
Accuracy on confident answers only: 0.62 (8/13)


C:\Users\joego\CodePath AI Eng Projects\Projects\applied-ai-system-project>pytest test_predict_reliable.py
============================================================================================ test session starts =============================================================================================
platform win32 -- Python 3.12.9, pytest-8.3.3, pluggy-1.5.0
rootdir: C:\Users\joego\CodePath AI Eng Projects\Projects\applied-ai-system-project
plugins: dash-3.3.0
collected 7 items                                                                                                                                                                                             

test_predict_reliable.py .......                                                                                                                                                                        [100%]

============================================================================================= 7 passed in 0.06s ==============================================================================================
```